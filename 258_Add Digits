class Solution {
    public int addDigits(int num) {

        if(num == 0) return 0;
        else if(num % 9 == 0) return 9;
        else return num % 9;
    }
}

// the first step is to list some examples, and then find the rule, which
//is relative to %9 , also we need to pay attention to special cases
