class Solution {
    public int hammingDistance(int x, int y) {
        return Integer.bitCount(x ^ y);
    }
}

//  public int hammingDistance(int x, int y) {

//         int count = 0;
//         int xor = x ^ y;

//         for (int i = 0; i < 32; i++) {
//             count += (xor >> i) & 1;
//         }

//         return count;
//     }
