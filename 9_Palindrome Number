class Solution {
    public boolean isPalindrome(int x) {

        String st = String.valueOf(x);
        int i = 0;
        int j = st.length() - 1;

        while(i < j){
            if(st.charAt(i) != st.charAt(j)) return false;
            i++;
            j--;
        }

        return true;
    }
}
